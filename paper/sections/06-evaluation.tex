\section{Preliminary Evaluation}

Four six-stage v1.02h replays are pinned by hash.  Three are tracked fixtures
from a public replay uploader; one world-record replay remains local because its
source prohibits secondary redistribution.  All four reached the
\code{replay-complete} terminal in the Linux compatibility runner.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}llrrr@{}}
    \toprule
    Replay & Route & Logic ticks & Stage 6 frame & Final score \\
    \midrule
    \code{th6\_ud000134} & Lunatic / Marisa B & 84,817 & 16,485 & 181,778,020 \\
    \code{th6\_ud000232} & Lunatic / Reimu B  & 86,676 & 16,727 & 136,978,560 \\
    \code{th6\_ud002677} & Normal / Reimu A, NN/NB & 85,759 & 17,283 & 172,519,700 \\
    \code{th6\_udLuRB}   & Lunatic / Reimu B  & 118,605 & 26,233 & 804,515,970 \\
    \bottomrule
  \end{tabular}
  \caption{Compatibility playback results.  NN/NB denotes no-miss,
  no-bomb.  These are Linux self-consistency measurements, not original-game
  equivalence results.}
  \label{tab:compatibility-results}
\end{table}

Two independent 1,000-tick runs of \code{th6\_ud002677} produced
byte-identical JSONL with SHA-256
\begin{center}
\small\ttfamily
d4606beb09ea9040b0e747d95b2dc7ebe70d36f5b5c152c8289b6074e69e899a.
\end{center}
On the initial host, full untraced runs completed in approximately
1.1--1.6 seconds and stayed below 40 MiB resident memory.  These timings are
environment-specific and are not a performance claim.

The Stage 6 final score is computed during gameplay: playback restores the
preceding stage's score at Stage 6 entry but does not restore Stage 6's own end
score.  Exact score agreement is therefore a useful end-to-end consistency
check.  It remains much weaker than frame-level state equivalence.

\subsection{Shipped-executable differential anchor}

The first retail experiment runs the pinned Japanese v1.02h executable under
Wine 11.0 and the tracked Normal Reimu A NN/NB replay.  A debugger verifies the
expected bytes at the menu, input, and frame-anchor addresses and samples at
\code{0x00420858}, immediately after \code{Chain::RunCalcChain}.  The Linux
runner writes its diagnostic row at the corresponding return boundary.  The
comparison covers 34 fields: route/frame metadata, complete replay input, RNG
seed and generation, score/resources/rank, Player state and raw binary32 x/y/z
bits, time-stop state, raw effective-rate bits, movement-area bounds, bomb
movement multipliers, and all four character movement speeds.

All 2,000 compared frames match.  This interval contains 27 distinct input
masks, advances RNG generation from 2 to 3,555, and reaches score 767,990.
Every retail anchor observes x87 control word \code{0x007f} and MXCSR
\code{0x00001fa0}.  The raw trace hashes, executable and replay identities,
environment versions, and compared-field list are sealed in the
proprietary-byte-free evidence artifact
\path{evidence/retail-reference-002677-2000-v1.json}.

The Wine environment requires two explicit diagnostic interventions.  Its
\code{timeGetTime} begins near host uptime while the game's last-frame global
begins at zero, so the probe equates those values before gameplay to avoid a
spurious multi-year catch-up loop.  XTest does not reach Wine DirectInput on
this host, so the physical input routine returns zero; ReplayManager then
overwrites the complete replay mask at its authentic calc priority.  The
per-frame input equality is checked rather than assumed.  These interventions
and the finite 34-field projection prevent promoting the result to whole-state
or whole-program equivalence, but they establish a real retail oracle for the
first core slice.

\subsection{Player-position sliced-kernel gate}

The extended anchor exposes every value consumed by the position arithmetic.
From its 2,000 post-calc rows, the first row supplies an initial observed
position and the remaining rows define 1,999 consecutive transitions.  A
compact fixed-width vector retains each input mask, time/player gate, raw
movement environment, previous x/y bits, and expected next x/y bits while
binding the target, replay, and retail-trace hashes in its header.

The integer PC24 microkernel matches all 1,999 transitions exactly.  Separate
unit tests exercise direction priority, focus speeds, clamps, signed zero,
ties-to-even, unsupported classes, and time-stop ordering.  A deterministic
50,000-pair normal-domain comparison also agrees with host binary32 add,
multiply, and ordering; host floating point is used only as a test oracle, not
by the kernel.  These results validate the reached implementation path but do
not prove the integer rounding algorithm, per-instruction control state,
instruction/source correspondence, reachable-domain invariant, or projection
noninterference.

\subsection{Hash-bound OpenVM application proof}

The shared microkernel compiles under OpenVM v2.0.1 for
\code{riscv32im-risc0-zkvm-elf}.  The guest validates a private 95,976-byte
workload, executes the 1,999 chained transitions, and uses the SHA-256 extension
to publish a digest of the domain separator, complete workload, and computed
final x/y bits.  Independent host construction, pure execution, metered
execution, and direct SDK deserialization of the application proof all yield
the same public digest:
\begin{center}
\small\ttfamily
21e195a5a5a123c01d9f48c876949340f6922e55215ced83d74dd06a988f84e6.
\end{center}

Table~\ref{tab:openvm-motion-meter} reports the CPU meter.  Work differs across
prefixes because many early frames take the time/player gates or contain no
movement, but the large-prefix cost is approximately linear.  The full
hash-bound workload executes 6,205,625 guest instructions and costs an
estimated 254,156,116 cells.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}rrr@{}}
    \toprule
    Transitions & Guest instructions & Metered cells \\
    \midrule
    1     & 3,595     & 152,867 \\
    10    & 24,836    & 1,031,394 \\
    100   & 251,352   & 10,366,661 \\
    1,000 & 3,157,630 & 129,282,130 \\
    1,999 & 6,205,625 & 254,156,116 \\
    \bottomrule
  \end{tabular}
  \caption{OpenVM v2.0.1 player-motion meter results with full SHA-256
  workload/result binding.}
  \label{tab:openvm-motion-meter}
\end{table}

On one AMD EPYC 9654 host with 192 logical CPUs, application proving completes
in 75.65 seconds, uses 2,545.45 user CPU seconds, and peaks at 50,308,612 KiB
resident memory.  Exact-commitment verification of the 872,364-byte proof takes
0.12 seconds and peaks at 49,092 KiB.  Verification with the expected guest
commitment succeeds; changing its last bit fails with an explicit executable-
commit mismatch.  The proof's authenticated public-value bytes were decoded
through the pinned SDK and match the digest above.  The proof, 241,614-byte
verifying key, 252,122-byte vm executable, and hash/source manifest are tracked
and verify without retail data or proving hardware.

The rejected reconnaissance statement cost 230,560,805 cells for the full
prefix.  Full transcript/result binding adds 10.2339\% and was retained because
the cheaper form admitted an uncommitted existential witness.  This experiment
establishes a real proof and an exact code/workload/result binding for the
slice.  It does not establish that the eleven private environment words follow
from prior TH06 state, nor does it evaluate zero-knowledge/privacy; the vector
is already public.  No whole-game or formal-refinement claim follows from this
backend measurement.

\subsection{Canonical projection experiment}

Two independent complete revision-0.2 traces of the Normal Reimu A NN/NB run
were byte-identical across all 85,759 frame records.  Both terminated at Stage
6 frame 17,283 with score 172,519,700.  Each trace occupied 50,769,392 bytes and
had SHA-256
\begin{center}
\small\ttfamily
ea0cdf948ba7668cba31064dfa421a9c279fdab28bdbd1d57c817ba13db84117.
\end{center}
Although only fixed-size digests were retained, each run hashed
6,192,210,130 bytes (5.767 GiB) of selected canonical payload.  Two concurrent
local executions took 31.90 and 32.04 seconds and peaked at 38,388 and 38,724
KiB resident memory.  This measures the current diagnostic implementation; it
does not estimate zkVM proving cost.

An earlier serializer hashed every named member of each Stage ANM VM.  Two
2,000-tick runs then disagreed in the Stage subsystem at record zero, while RNG
and all other subsystem digests agreed.  Inspection showed that Stage VMs are
allocated with \code{malloc} and that disabled interpolation members are not
initialized.  Encoding those members only when their control mode can read
them restored byte identity for both the 2,000-tick and complete runs.  This is
a concrete example of why a canonical state must follow future-read semantics
rather than C++ object layout.

The complementary inactive-state failure is machine-checked in a small Lean
model.  Two free Effect slots with equal active-only projections but distinct
raw radius bits evolve to distinct projected states under the same spell-orbit
allocation.  A second theorem shows that retaining dormant radius and angle
makes this modeled allocation phase commute.  The proof compiles with Lean
4.32.0 without \code{sorry}, admitted results, or custom axioms.  Its scope is
only this allocation phase and does not establish sufficiency for all Effect
callbacks or bind the model to compiled C++.

\subsection{Static arithmetic census}

The arithmetic audit binds the executable, authoritative function map, and
implemented-function list by SHA-256.  GNU \code{objdump} 2.38 linearly decoded
133,078 instruction lines in the complete image, including 10,100 x87 lines.
Of these, 9,522 lie in mapped ranges.  Complete-image counts are static upper
bounds: they include CRT, D3DX, and potentially unreachable code rather than
dynamic execution frequencies.

The mapping attributes 5,980 x87 instructions to 159 \code{th06::} functions,
all marked implemented by the pinned reconstruction metadata.  Table~\ref{tab:arithmetic-census}
summarizes the source-binding candidate.  The 26 direct transcendental sites
are all \code{fsincos}.  The store widths show why rounding every source
operation to binary32 would also be wrong: x87 intermediates can remain live
until a specific DWORD or QWORD store.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}lr@{}}
    \toprule
    Static property in mapped game functions & Count \\
    \midrule
    x87 instructions & 5,980 \\
    direct x87 control/environment instructions & 0 \\
    \code{fsincos} instructions & 26 \\
    \code{frndint} instructions & 10 \\
    \code{fcomp} instructions & 244 \\
    DWORD/QWORD \code{fcomp} operands & 236 / 8 \\
    x87 stores & 1,793 \\
    DWORD stores & 1,713 \\
    QWORD stores & 80 \\
    \bottomrule
  \end{tabular}
  \caption{Static x87 census for mapped \code{th06::} ranges.  Counts are
  sites, not execution frequencies or a proved gameplay slice.}
  \label{tab:arithmetic-census}
\end{table}

Every mapped comparison is followed immediately by \code{fnstsw ax}, one of
six observed masks, and a conditional branch.  The 244 sites form eleven
complete consumer signatures.  Their greater/less/equal/unordered truth tables
are checked in Lean, making the exact branch observation---including unordered
behavior---an explicit extraction obligation rather than a guessed C++
relation.

Function-body counts alone are incomplete.  The audit also finds 163 direct
calls from mapped game code into 18 x87 helpers or wrappers: 77 call
\code{\_\_ftol2}, 16 each call \code{sin} and \code{cos}, 11 call
\code{sqrt}, and seven call \code{atan2}.  The complete image contains 356
XMM-operand instruction lines, none inside a mapped \code{th06::} range; they
belong to a Pentium-4 \code{pow} implementation, a CRT helper used by D3DX
rasterization, and feature detection.  Their removal still requires a
transitive noninterference argument for the corresponding draw paths.

At the 77 \code{\_\_ftol2} sites, predecessor counts are 60 \code{fld}, seven
\code{fmul}, four \code{fdiv}, and two each of \code{fadd}, \code{fsubp}, and
\code{fsubr}.  A bounded straight-line scan observes EAX at every caller and
EDX at none.  The bit-mask refinement records 75 full-EAX and two AL-only
observers.  It stops at 15 control-flow boundaries, 34 subsequent calls, and
28 syntactically resolved windows; EDX remains live at 42 stops.  This
identifies a candidate observable projection but is not complete data-flow
analysis and does not establish a reachable input range.

The CRT trigonometric wrappers embed raw x87 control word \code{0x027f}.  A
small Lean definition checks its 53-bit significand precision,
round-to-nearest-even mode, and six masked exception bits.  Static inspection
did not find a game-body instruction that establishes this word at entry, so
the result is deliberately a wrapper-profile fact rather than an environment
theorem.  The retail anchor observes \code{0x007f}, selecting 24-bit precision,
at every post-calc sample.  That observation rules out treating \code{0x027f}
as one global precondition but does not expose transient words inside a frame.
A fuller exporter must record or extract the entry and per-site profile changes
and processor profile before exact transcendental equivalence can be claimed.

\subsection{Arithmetic-boundary oracle experiment}

The arithmetic experiment compares x87 and Berkeley SoftFloat Release 3e for
add, subtract, multiply, divide, and square root under the decoded PC53
nearest-even profile.  It also compares binary32/binary64 \code{fstp},
\code{frndint}, and signed 32/64-bit \code{fistp} under nearest-even and the
audited D3DX toward-zero profile.  The runner exports exact upstream commit
\code{f74b1e48110ac3a27dd49b787d164e55e42d81d1}, verifies selected file hashes,
and compiles only the 37 required translation units with the SoftFloat
\code{8086} specialization.  The probe source for this run had SHA-256
\begin{center}
\small\ttfamily
b062a031b5866b7e86514db17d162ecbdf9398b0a5d0c5530dfcf6c4889ffe71.
\end{center}

Because the first retail boundary measurement is PC24 rather than PC53, this
campaign does not yet validate the ambient game-body profile.  A corresponding
\code{0x007f} campaign is now required; the PC53 result remains evidence for
the wrapper profile and extended-range stress cases.

Raw result representations and x87 exception bits 0--5 are compared.  The
SoftFloat flags map invalid, divide-by-zero, overflow, underflow, and inexact;
the x87-only denormal-operand bit is supplied by an instruction-specific rule.
The first such rule---set the bit whenever either arithmetic input is
subnormal---failed on a fixed subnormal-dividend/zero-divisor case.  Following
the documented exception priority and instruction-specific \code{frndint},
\code{fistp}, and \code{fstp} behavior repaired the model before the reported
campaign.  A finite-class Lean definition checks both counterexamples and the
boundary distinction, but does not prove that the manuals, hardware, or
SoftFloat implement that definition.

The comparison campaign additionally mirrors \code{fcomp m32fp} and
\code{fcomp m64fp}.  It checks C0, C1, C2, C3, and exception bits using
finite random pairs plus fixed infinities, quiet/signaling NaNs, signed zeros,
and invalid-versus-denormal priority cases.  A separate Lean definition checks
the eleven reached status-mask/branch truth tables, without claiming hardware
or binary correspondence.

On an AMD EPYC 9654 with GCC 11.4.0, one million deterministic pseudorandom
cases per operation, profile, and input family, plus fixed boundary cases,
yielded Table~\ref{tab:softfloat-probe}.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Family & Tuple set A & Tuple set B & Mismatches \\
    \midrule
    Five basic operations & 5,000,798 & 5,001,314 & 0 \\
    Five boundaries, two RC profiles & 10,000,140 & 10,000,330 & 0 \\
    \code{fcomp m32fp}/\code{m64fp} & 2,000,110 & 2,000,110 & 0 \\
    \bottomrule
  \end{tabular}
  \caption{Host-specific x87/SoftFloat result/condition/exception
  counterexample search.  Sets A/B are binary32-derived/PC53 extended for the
  first two rows and m32fp/m64fp memory operands for comparisons.}
  \label{tab:softfloat-probe}
\end{table}

All six exception bits occurred in the basic campaign: 1,001,228 invalid,
88,466 denormal-operand, 56 divide-by-zero, 255,746 overflow, 265,276
underflow, and 7,547,221 inexact observations.  Counts overlap when an
instruction sets multiple bits.  The comparison campaign observed 76 invalid
and 29,682 denormal-operand flags; its relation counts were 2,001,320 greater,
1,998,808 less, 16 equal, and 76 unordered.  Across all campaigns, 34,002,802
tuples matched and the measured run took 3.6 seconds.

Canonical PC53 inputs retain the full extended exponent range, set the
explicit integer bit consistently with the exponent, and clear the eleven
significand bits below the selected precision.  The zero-mismatch result makes
SoftFloat a plausible oracle for this subset on the measured host; it proves
neither implementation correct.  TOP/tag state, arbitrary NaN payloads and
noncanonical encodings, \code{fld m32fp} denormal signaling, remainder,
transcendental instructions, dynamic reachability, and model-to-code binding
remain outside the experiment.

\subsection{Extracted \code{\_\_ftol2} experiment}

The helper occupies 117 bytes at virtual address \code{0x0045ba78}; the local
extractor binds its body by SHA-256 after first verifying the complete retail
image.  It retains the bytes only in a temporary directory and links them into
a freestanding i386 stream harness, so neither the executable fragment nor a
derived object is redistributed.

An independent Python decoder computes truncation toward zero directly from
the sign, exponent, and significand of canonical finite ext80 values.  On the
same host, 1,000,014 fixed and deterministic PC53 inputs in the signed-i64
domain produced identical complete EDX:EAX values; every invocation returned
the x87 stack to empty, and 908,273 cases left the inexact flag set.  The run
took 3.5 seconds.  A Lean case model separately checks the helper's
nearest-even carry/borrow correction and the EDX:EAX split/join identity.

A second campaign queried only the observation used by ECL variable dispatch.
Its 1,000,154 cases include quarter-step boundaries around IDs $-10025$ through
$-10001$, dense deterministic PC53 samples, infinities, quiet/signaling NaNs,
large finite values, a pseudo-denormal, and an unnormal.  All classifications
matched; 3,084 operands selected a variable ID and nine cases set invalid.
All nine invalid cases returned the complete integer-indefinite result
\code{0x8000000000000000}.  The combined campaign therefore executed the exact
helper 2,000,168 times.

The signed-i64 truncation result is deliberately claimed only for its tested
canonical finite domain.  The nine sampled invalid results and the classifier
campaign are finite counterexample search, not universal exceptional
semantics.  A separate byte-free artifact checks all 37 helper instruction
signatures and records the masked-invalid path; a Lean bit-vector theorem
checks that integer-indefinite projects to EAX zero and EDX
\code{0x80000000}.  Those results remain conditional on verified decoding,
architectural x87 semantics, entry stack/control state, branch binding, caller
binding, and guest refinement.  General integer consumers still require their
site-specific range or observation invariants.  The ECL call instead requires
exact agreement on the 25 sentinel outcomes before the helper can be replaced
by a smaller classifier in the proof kernel.

\subsection{Address-level obligation ledger}

The two audited families are frozen as 321 sorted, unique address records in a
canonical JSON ledger.  The artifact accepts only the pinned executable,
mapping, and implemented-list hashes; it records hashes of its generator and
audit tool and seals its canonical content with SHA-256.  A public unit test
checks that seal, the complete count/signature distributions, conservative
status fields, and the absence of full disassembly operands without requiring
the commercial input.

This is progress in traceability, not proof completion.  All 244 comparison
records remain \code{unclassified} with respect to the gameplay slice.  All 77
helper records remain \code{unclassified} and
\code{reachable\_signed\_i32\_range = unproved}.  Each retained record must
eventually name a binary-to-model and guest-refinement theorem; each omitted
record must name a projection-noninterference theorem.

\subsection{Source candidates and an observation quotient}

A derived ledger covers all 77 helper calls with source-expression candidates
anchored to 16 files at authoritative revision
\code{cc475a0bc3fef38683b0f02224c87ddba0a021d9}.  It classifies 68 calls as
omission-after-noninterference candidates and nine as retained: one ECL
variable-dispatch call and eight point-item scoring calls.  The largest sink
classes are 28 D3D viewport conversions and 20 text-raster coordinates.  The
method combines function mapping, local disassembly dataflow, source order,
and pinned line anchors.  It is explicitly marked as manual, unproved
correspondence, and no omission is yet admitted.

The ECL call at \code{0x0040b38b} admits a smaller observation than an arbitrary
integer result.  A hash-bound artifact checks 17 local instruction signatures
and extracts 25 unique in-function jump targets.  The decoded code adds 10025
modulo $2^{32}$ and accepts unsigned values at most 24, corresponding to ECL
variable IDs $-10025$ through $-10001$; every other value returns the original
float literal.  Lean's bit-vector decision procedure checks the machine
interval identity, and an abstract theorem checks irrelevance of the exact
non-label integer.  These results narrow, but do not discharge, four remaining
obligations: verified decode, universal helper/classifier agreement, target-to-
variable binding, and guest refinement.  The eight retained scoring calls'
player-candidate, writer/scheduling, collision, helper-observation, and
code-binding obligations remain open and are decomposed next.

\subsection{Fixed-data ECL alias-writer audit}

The ECL variable resolver returns pointers for the player-position IDs and
marks them readonly.  Most output handlers honor this type, but retail opcodes
18 and 19 increment or decrement the returned pointer directly.  Consequently,
a source search for explicit player assignments is not a complete writer
inventory: a fixed script could name player y as an aliased output.

The new artifact binds the executable, mapping, data manifest, stage archive,
and variable-dispatch audit.  A pinned thtk Release 12 extraction~\cite{thtk}
is checked by the hashes of all seven ECL outputs.  An independent parser then
walks 321 subroutines, their 321 sentinels, and 8,384 instructions.  It
identifies 1,844 candidate output operations; none names a readonly or
player-position ID.  Of
these, 80 are direct increment/decrement operations that bypass the type guard.
Their destination support is only four local IDs: $-10012$, $-10004$,
$-10002$, and $-10001$.  The artifact also checks 28 retail instruction
signatures and 17 source anchors.  A Lean policy theorem reduces a modeled
player-y alias write to one of the unchecked opcodes and checks the transcribed
four-ID support.

This result is conditional.  The thdat extraction is not proved equivalent to
the retail PBG3 loader; the independent walk is not yet connected to the
runtime decoder; handler classification, compiler correspondence, instruction
immutability, non-ECL alias completeness, artifact-to-Lean binding, and guest
refinement remain open.  It nevertheless removes a concrete fixed-data
counterexample family without assuming that the readonly guard is universally
obeyed.

\subsection{Player-position clamp decomposition}

A dependent artifact checks 95 exact instruction signatures across eight
mapped functions, 21 source anchors at the pinned authoritative revision, five
binary32 constants, four character-speed records, and four comparison sites
from the base ledger.  It covers initialization and respawn at center
$y=384$, movement-speed construction, bomb and effective-frame multipliers,
the ordered clamp to $[16,432]$, radius-12 grab-box construction, and the
vertical item-collision branches.  The point-item artifact seals this player
artifact's digest as a direct input, and the player artifact in turn seals the
fixed-data ECL writer artifact.

The exceptional cases make the invariant smaller than expected.  Negative
infinity satisfies the lower comparison and is assigned 16; positive infinity
satisfies the upper comparison and is assigned 432.  NaN makes each comparison
unordered and takes the two audited parity branches that skip assignment.  A
total Lean theorem therefore proves that the clamp yields a finite bounded
center for every candidate except NaN.  Further theorems check initial and
respawn boundedness and derive grab-top and grab-bottom ranges $[4,420]$ and
$[28,444]$.

This does not yet prove the reachable player invariant.  The remaining queue
contains verified decode and source correspondence, GameManager
reinitialization, the ECL extraction/parser/handler bindings, non-ECL direct
and aliased writer coverage, finite movement operands and non-NaN candidate
construction, binary32 store and x87 comparison semantics, Player-before-Item
scheduling, and guest refinement.  In particular, the audit records adaptive
effective-frame-multiplier publishers and does not replace them with a
constant-one assumption.

\subsection{Point-item range decomposition}

The eight scoring calls are grouped into four difficulty profiles.  A pinned
binary artifact checks 77 key instruction signatures: the five-entry
difficulty dispatch, two binary32 loads of the same
\code{Item.currentPosition.y} field per profile, both helper calls, signed
threshold 128, profile constants, 32-bit penalty arithmetic, joins, and the
final gameplay-score addition.  This is static decode evidence, not verified
translation.

The apparent range argument contains a NaN trap.  The collision function
returns false when any ordered comparison proves the player and item boxes
separate.  If an item coordinate is NaN, each such comparison is false and the
function can instead return collision success.  Therefore collection alone is
not a finiteness witness.  Rather than assume item finiteness, the total model
splits its binary32 coordinate into exact finite rationals, both infinities,
and NaN.  With finite player center $y\in[16,432]$, player grab radius 12, and
item half-size 8, a collected finite item lies in $[-4,452]$ and truncation
toward zero preserves that interval.  Either infinity is vertically separated
from the finite player box.  NaN may fall through to collision success, but
masked-invalid \code{fistp} produces integer-indefinite; the audited scoring
callers observe EAX zero and select the top-score branch.  Across this total
case split the Lean theorem bounds all four score formulas by
$[27600,300000]$; their largest finite position penalty is 129600 and every
selected score fits signed i32.  Given a pre-item score at most 999,999,999, a
further theorem rules out u32 wrap in one award.  This theorem is conditional
on discharge of the player-position artifact and exact collision/helper
observations.
Difficulty $0..4$ is an additional reachable-state premise because the decoded
invalid path bypasses profile assignment.

For invariant discovery, a debug Linux runner was executed under GDB with raw-
binary32 breakpoints at all four source score calls.  The sealed report binds
the probe, runner, source file, retail-data manifest, and replay hashes.
Table~\ref{tab:item-score-range} summarizes the measurements.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    Replay & Collections & Minimum $y$ & Maximum $y$ & Top / position \\
    \midrule
    \code{th6\_ud000134} & 403 & 11.879809 & 442.976013 & 242 / 161 \\
    \code{th6\_ud000232} & 304 & 26.522402 & 437.142883 & 131 / 173 \\
    \code{th6\_ud002677} & 599 & 14.489738 & 421.830170 & 417 / 182 \\
    \code{th6\_udLuRB}   & 775 & 13.629821 & 430.869507 & 623 / 152 \\
    \midrule
    Total                 & 2,081 & 11.879809 & 442.976013 & 1,413 / 668 \\
    \bottomrule
  \end{tabular}
  \caption{Collected point-item inputs in the Linux reconstruction.  All 2,081
  values were finite, truncated to 11 through 442, and stayed within the
  candidate $[-4,452]$ interval.  This is corpus counterexample search, not a
  reachable-state proof or retail-executable result.}
  \label{tab:item-score-range}
\end{table}

No sample was exceptional or outside the candidate interval.  This supports
the finite branch and gives both score branches substantial coverage, but the
total theorem does not use corpus absence of exceptional item values as a
premise.  The experiment does not discharge the player candidate, writer,
scheduling, or code-binding obligations, original-binary collision/helper
correspondence, exact x87 refinement, or guest binding.
